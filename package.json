{
  "name": "subdude",
  "version": "0.1.0",
  "description": "Extension for pi-coding-agent that provides a subdued agent experience, with an advisor mode that includes read-only exploration, writing findings to SUBDUDE.md, and a edit-enabled mode that requires approvals before changes.",
  "keywords": [
    "pi-package"
  ],
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "files": [
    "src/"
  ],
  "scripts": {
    "check": "tsc --noEmit"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "peerDependencies": {
    "@mariozechner/pi-ai": "*",
    "@mariozechner/pi-coding-agent": "*",
    "@mariozechner/pi-tui": "*",
    "typebox": "*"
  },
  "dependencies": {
    "diff": "^8.0.2"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
